run_id: ribosome-rinalmo-micro-pretrained-v1

run_tasks:
  - reconstruction

dataset:
  dataset_id: ribosome
  prepared_dir: data/prepared/ribosome

model:
  adapter: rinalmo
  size: micro
  weights: models/rinalmo/rinalmo_micro_pretrained.pt
  device: cuda:0

reconstruction:
  context_length: 512
  batch_size: 8

outputs_root: outputs/runs
